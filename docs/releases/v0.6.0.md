# SXPE 0.6.0 (M6) — draft release notes

**Tag (coordinator):** `v0.6.0`  
**Asset (coordinator):** Windows portable zip from `package.bat` / `scripts/package.ps1`  
(`dist/sxpe-0.6.0-windows-x64.zip`).

This file is a draft for the GitHub Release body. Creating the git tag and uploading the asset is **out of scope** for the docs PR; do that after merge to `dev`.

## Highlights

- Daily-driver package editor milestone (M6): validate report, package compare, find references, XML/ITUN and NMAP editors, Downloads folder scan, neighborhood/world/DBC layout-lock honesty, S3SA external viewer, read-only Sims3Pack inspect, Linux Qt GUI smoke path.
- **Help → Check for update** queries `api.github.com/repos/tofb15/sxpe/releases/latest`, compares to `SXPE_VERSION`, and never auto-downloads.
- Documentation overhaul: README quick starts, `docs/user-guide.md`, `docs/cli-mcp.md`, `docs/workflows.md`, building/release notes.

## Install (Windows portable)

1. Download `sxpe-0.6.0-windows-x64.zip` from this Release.
2. Unzip; keep DLLs and plugin folders next to the executables.
3. Run `SXPE.bat`.

CLI: `sxpe-cli.bat`. MCP: `sxpe-mcp.bat`.

## Building from source

See [docs/building.md](../building.md). Version sources: `CMakeLists.txt` `PROJECT_VERSION` and `vcpkg.json` `"version-string"` (both **0.6.0**).

## Limits (unchanged honesty)

- Sims 3 only; no Sims 4 profile.
- No full 3D mesh / CLIP playback.
- No Store/DRM Sims3Pack.
- Layout-locked `.nhd` / `.world` / `.dbc` (safe in-place replace only).
- No third-party GUI plugins.

## Tracker

M6 roadmap: [#33](https://github.com/tofb15/sxpe/issues/33). Update check / Releases: [#28](https://github.com/tofb15/sxpe/issues/28).
