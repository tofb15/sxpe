# SXPE 0.7.0 (M7 / community) - release notes

**Tag:** `v0.7.0`

Published on GitHub as a **pre-release / Beta**.

## Published assets

| Asset | Status |
| --- | --- |
| `sxpe-0.7.0-windows-x64.zip` | **On this Release** - portable GUI + CLI + MCP + Qt plugins + MSVC CRT. Unzip and run `SXPE.bat`. |
| `sxpe-0.7.0-windows-x64-cli.zip` | **On this Release** - Windows CLI + MCP only (no Qt / no `sxpe_gui`). Unzip and run `sxpe-cli.bat` / `sxpe-mcp.bat`. |
| `sxpe-0.7.0-linux-x64-cli.tar.gz` | **On this Release** - Linux CLI + MCP (no Qt). Rebuild with `scripts/package-linux.sh --no-gui`. |
| `sxpe-0.7.0-linux-x64.tar.gz` | **On this Release** - Linux CLI + MCP + `sxpe_gui`. Qt is **not** vendored; system or official Qt 6.5+ must resolve at runtime. |

## Highlights

M7 / community work landed on `dev` since 0.6.0:

- Large-merge resilience (caps, progress, temp hygiene)
- Merge conflict hygiene (leftovers, duplicate TGI policy)
- Huge-package open performance (index-first, caps, RO threshold)
- Long-op cancel + rollback for merge/import
- First-run / merge onboarding for non-experts
- Game/file lock detection before open and save
- Broader DDS decode/replace coverage
- Typed OBJD/CASP editors with dryRun + undo
- REFS editor and outbound listRefs
- Limited Sims3Pack pack authoring
- Richer RCOL summary and safe chunk replace
- CLIP metadata editor, safe fields, batch exportAs
- Third-party GUI plugins permanently unsupported (#60)
- Linux packaging + docs parity with Windows (#52)

## Install

### Windows portable (GUI)

1. Download `sxpe-0.7.0-windows-x64.zip` from this Release.
2. Unzip; keep DLLs and **Qt** plugin folders (`platforms/`, etc.) next to the executables.
3. Run `SXPE.bat`. CLI: `sxpe-cli.bat`. MCP: `sxpe-mcp.bat`.

Local rebuild: `package.bat` / `scripts/package.ps1` (see [docs/building.md](../building.md)).

### Windows CLI + MCP

1. Download `sxpe-0.7.0-windows-x64-cli.zip` from this Release.
2. Unzip; run `sxpe-cli.bat` / `sxpe-mcp.bat` (no Qt DLLs in this zip).

Local rebuild: `scripts/package.ps1 -NoGui` (or `-CliOnly`).

### Linux CLI + MCP

1. Download `sxpe-0.7.0-linux-x64-cli.tar.gz` from this Release (or rebuild with `./scripts/package-linux.sh --no-gui`).
2. Extract and run `./sxpe` / `./sxpe_mcp` (or the `*-cli.sh` / `*-mcp.sh` launchers).

### Linux GUI

1. Download `sxpe-0.7.0-linux-x64.tar.gz` from this Release (or package with `./scripts/package-linux.sh` when `sxpe_gui` was built).
2. Extract and run `./SXPE.sh` / `./sxpe_gui`.
3. **Honest limit:** this tarball does **not** vendor Qt shared libraries. Install Qt 6.5+ (distro `qt6-base` packages or an official `gcc_64` kit) so `libQt6Widgets` / Network resolve. See [docs/building.md](../building.md).

## Building from source

See [docs/building.md](../building.md). Version sources: `CMakeLists.txt` `PROJECT_VERSION` and `vcpkg.json` `"version-string"` (both **0.7.0**).

## Known limits

- Sims 3 only; no Sims 4 profile.
- No full 3D mesh / CLIP playback.
- No Store/DRM Sims3Pack.
- Layout-locked `.nhd` / `.world` / `.dbc`.
- Third-party GUI plugins permanently unsupported (#60).

## Trackers

Closed: [#54](https://github.com/tofb15/sxpe/issues/54) (Windows zip on Releases), [#52](https://github.com/tofb15/sxpe/issues/52) (parity), [#53](https://github.com/tofb15/sxpe/issues/53) (docs hygiene), [#60](https://github.com/tofb15/sxpe/issues/60) (plugins), [#62](https://github.com/tofb15/sxpe/issues/62) (M7), [#69](https://github.com/tofb15/sxpe/issues/69) (community research). Update check / Releases history: [#28](https://github.com/tofb15/sxpe/issues/28). Prior notes: [v0.6.0](v0.6.0.md).
